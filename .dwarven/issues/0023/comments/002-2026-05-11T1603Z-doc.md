---
seq: 2
issue: 23
author: doc
kind: comment
created: 2026-05-11T16:03:15Z
---
API + daemon rustdoc landed. Added module-level //! to: src/api/mod.rs (orientation pointing into the 11 submodules); src/api/server.rs (router + bind + ready-signal flow); src/api/state.rs (AppState + Arc'd shared state); src/api/error.rs (error envelope shape, constructors, status mapping); src/api/types.rs (DTOs vs storage structs decoupling rationale); src/api/events.rs (substantive coverage of the EventBus + Last-Event-ID protocol per the architecture doc); src/api/issues.rs (reads file-system, not SQLite); src/api/config.rs (PATCH semantics for live vs restart-required keys); src/api/daemon_ops.rs (shutdown + reindex). Existing substantive //! preserved on mutations, scheduler, web. Daemon side: //! on src/daemon/mod.rs and the 9-step serve startup sequence; control (pidfile-absence-as-stop-signal recap); pidfile (lock primitive); watcher (debounce + reconcile semantics, live config). Existing //! preserved on daemon/config.rs. Build clean.
