---
seq: 2
issue: 24
author: doc
kind: comment
created: 2026-05-11T16:05:57Z
---
Crate-root + small-module rustdoc landed. Added: substantive //! to src/lib.rs (the new entry-point doc — describes both build targets, points to docs/architecture/overview.md, lists every module with one-paragraph summary); src/main.rs (binary entry point + CLI/daemon orientation); src/init.rs (init scaffold + idempotency); src/config.rs (the CLI config surface, distinguishing it from the three other config.rs files in different module paths); src/time.rs (the two ISO-8601 forms). eval/* modules already had substantive //! headers — preserved. Also fixed eight intra-doc-link warnings surfaced by cargo doc --no-deps: replaced references to private items (ROSTER, router) with the public function alternatives (roster(), spawn()); escaped non-link bracketed refs to coordination-hub.md#R3.3 and rename(2). cargo doc --no-deps now produces zero warnings. Build clean; this closes the code-documentation arc (#18-#24).
