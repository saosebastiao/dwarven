# STALE — v0.1 inherited content

The slash command definitions in this directory implement the **v0.1 architecture**: thin wrappers dispatching v0.1 subagents that coordinate via GitHub.

The project pivoted on 2026-05-09. See `CLAUDE.md` and `docs/specs/dwarven.md` for the v2 architecture.

**Do not refactor in place.** v1 implementation will materialize fresh slash commands via the Claude Code adapter (`docs/specs/host-adapter.md#R3.2`).

For historical context, read these files. For current behavior, defer to `docs/specs/`.
